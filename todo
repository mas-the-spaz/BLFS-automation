add sys.argv option to install without a flag?
suppress keystroke during a subprocess
add list of installed programs?
    during each build phase, check if package is installed
    if yes - skip
    if not proceed and add it to the list
    during listing check install-list if package is written in 
    
add custom installation instruction

hash index out of range - hashing when tar file is after patch file